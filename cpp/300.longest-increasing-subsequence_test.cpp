#include "leetcode.h"
#include <gtest/gtest.h>

#include "300.longest-increasing-subsequence.cpp"

#define FunName lengthOfLIS

TEST(testname, subname) {
  VERIFY(4, {0,1,0,3,2,3});
  VERIFY(1, {1,1,1});
}
