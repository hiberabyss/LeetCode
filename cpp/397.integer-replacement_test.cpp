#include "leetcode.h"
#include <gtest/gtest.h>

#include "397.integer-replacement.cpp"

#define FunName integerReplacement

TEST(testname, subname) {
  VERIFY(1, 2);
  VERIFY(2, 4);
  VERIFY(4, 7);
  VERIFY(32, 2147483647);
}
