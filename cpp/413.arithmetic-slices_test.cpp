#include "leetcode.h"
#include <gtest/gtest.h>

#include "413.arithmetic-slices.cpp"

#define FunName numberOfArithmeticSlices

TEST(testname, subname) {
  VERIFY(0, {1});
  VERIFY(3, {1, 2, 3, 4});
}
