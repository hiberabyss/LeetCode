#include "leetcode.h"
#include <gtest/gtest.h>

#include "223.rectangle-area.cpp"

#define FunName computeArea

TEST(testname, subname) {
  VERIFY(45, -3, 0, 3, 4, 0, -1, 9, 2);
  VERIFY(16, -2, -2, 2, 2, -2, -2, 2, 2);
  VERIFY(24, -2, -2, 2, 2, 1, -3, 3, 3);
}
